#!/usr/bin/env python

# A trivial module imported by the main cylc command to test that the cylc
# environment has been configured. Using any non-trivial module is
# dangerous because bugs introduced during development, say, will result
# in the main cylc command aborting immediately (during the import test).

pass
