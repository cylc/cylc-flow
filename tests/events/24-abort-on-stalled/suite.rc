[general]
    UTC mode = True # Ignore DST
    [[events]]
        abort on stalled = true
    [[reference test]]
        allow task failures = true
        live mode suite timeout=PT1M
[scheduling]
    [[graph]]
        R1 = foo => bar => baz

[runtime]
    [[foo,baz]]
        script = true
    [[bar]]
        script = false
