[cylc]
    [[event hooks]]
        startup handler = "/bin/false"
        abort if startup handler fails = True

[scheduling]
    [[dependencies]]
        graph = "foo"

[runtime]
    [[root]]
        script = "/bin/true" # no sleep
