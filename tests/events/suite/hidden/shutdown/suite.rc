[general]
    [[events]]
        shutdown handler = "/bin/false"
        abort if shutdown handler fails = True

[scheduling]
    [[graph]]
        R1 = "foo"

[runtime]
    [[root]]
        script = "/bin/true" # no sleep
