[cylc]
    [[events]]
        shutdown handler = "/usr/bin/false"
        abort if shutdown handler fails = True

[scheduling]
    [[graph]]
        R1 = "foo"

[runtime]
    [[root]]
        script = "/usr/bin/true" # no sleep
