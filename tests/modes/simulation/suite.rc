[cylc]
    force run mode = simulation
    [[reference test]]
        simulation mode suite timeout = PT1M
[scheduling]
    [[dependencies]]
        graph = "a => b => c"
[runtime]
    [[a, b, c]]
        [[[simulation mode]]]
            run time range = PT1S,PT2S
