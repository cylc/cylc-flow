[cylc]
   [[reference test]]
       required run mode = live
       live mode suite timeout = 0.5 # minutes
[scheduling]
    [[dependencies]]
        graph = """a => b \
                     => c"""
[runtime]
    [[a,b,c]]
        script = true
