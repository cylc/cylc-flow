[meta]
    title = "Job script: no directives test for SLURM"

[cylc]

[scheduling]
    [[dependencies]]
        graph = foo & bar%bar

[runtime]
    [[foo]]
        script = echo hello
        [[[job]]]
            batch system = slurm
    [[bar%bar]]
        script = echo hello
        [[[job]]]
            batch system = slurm
