[cylc]
   [[reference test]]
       required run mode = live
       live mode suite timeout = PT10S

[scheduling]
    [[dependencies]]
        graph = t1

[runtime]
    [[t1]]
        script = test "${CYLC_TASK_SUBMIT_NUMBER}" -eq 2
        retry delays = P0Y
