[scheduler]
    [[events]]
        stall timeout = PT0S
        abort on stall timeout = True

[scheduling]
    cycling mode = integer
    initial cycle point = 1
    [[graph]]
        R1 = a

[runtime]
    [[a]]
        platform = fake-platform
        submission retry delays = 1*PT1S
