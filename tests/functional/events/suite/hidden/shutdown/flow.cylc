[scheduler]
    [[events]]
        shutdown handler = "false"
        abort if shutdown handler fails = True

[scheduling]
    [[graph]]
        R1 = "foo"

[runtime]
    [[foo]]
        script = "true" # no sleep
