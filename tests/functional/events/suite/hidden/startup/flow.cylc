[scheduler]
    [[events]]
        startup handler = "false"
        abort if startup handler fails = True

[scheduling]
    [[graph]]
        R1 = "foo"

[runtime]
    [[foo]]
        script = "true" # no sleep
