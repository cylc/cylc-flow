[meta]
    description = """
This workflow is supposed to time out and abort when the timeout handler
deliberately fails."""

[scheduler]
    [[events]]
        workflow timeout = PT5S
        workflow timeout handler = "false"
        abort if workflow timeout handler fails = True

[scheduling]
    [[graph]]
        R1 = "foo"

[runtime]
    [[foo]]
        script = false
