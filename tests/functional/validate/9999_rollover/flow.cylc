# A workflow that tries to run beyond year 9999 without using extended year digits.

[scheduler]
    UTC mode = True
[scheduling]
    initial cycle point = 99991231T2200
    [[graph]]
        R3//PT1H = "foo"
[runtime]
    [[foo]]
        script = true
