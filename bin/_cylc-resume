#!/usr/bin/python

import sys
from connectors import connect_to_control


usage = """cylc resume [options] SYSTEM 

Resume running a paused system."""

connector = connect_to_control( usage )

connector.parse_args()

controller = connector.get_control()

if connector.prompt( 'Resume' ):
    controller.resume()
