The `cylc cat-log` command now supports a new `tail-end` mode (accessible via `cylc cat-log --mode tail-end` or `-m te`) which displays the most recent lines of a log file instead of following from the start. This complements the traditional `tail` mode which follows from the beginning of the file.
