Cylc will now kill and remove any tasks which were removed from the workflow
graph as the result of a reload or restart. Previously, tasks which were
active before the graph change would remain in the workflow, but could cause
problems.
