Tasks before the start cycle point now immediately return succeeded when queried by a workflow state xtrigger or the `cylc workflow-state` command. This prevents some warm-started workflows getting stuck waiting for earlier tasks that won't exist.
