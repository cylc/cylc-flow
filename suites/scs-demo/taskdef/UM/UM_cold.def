# THIS IS A CYLC TASK DEFINITION FILE


%TASK
    UM_cold

%DESCRIPTION
    Oneoff forecast from UM global dump to generate NZ warm start input 
    files (IAU start dump, Cx background). 

%TYPE
    free, oneoff

%HOURS
    6

%COMMAND
    UM_nz_coldstart.sh

%PREREQUISITES
    "global model dump ready for $(CYCLE_TIME)"

%OUTPUTS
    "IAU start dump ready for $(CYCLE_TIME)"
    "Cx background file ready for $(CYCLE_TIME)"
    "UM_nz restart files ready for $(CYCLE_TIME)"
