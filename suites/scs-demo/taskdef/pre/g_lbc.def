# THIS IS A CYLC TASK DEFINITION FILE


%TASK
    g_lbc

%DESCRIPTION
    Get the downloaded NZ LBC file for the next two cycles

%TYPE
    free, contact

%CONTACT_DELAY
    4.0 hr

%HOURS
    0,12

%INTERCYCLE
    True

%COMMAND
    file_transfer.sh

%ENVIRONMENT
    TARG ftpserver:/download/lbc_$(CYCLE_TIME - 6).um
    DEST hpc:/oper/input/

%COLDSTART_PREREQUISITES
    # make sure this task waits on the oneoff 06Z startup task 
    if HOUR in 0:
        "startup%$(CYCLE_TIME - 18) finished"
    if HOUR in 12:
        "startup%$(CYCLE_TIME -  6) finished"

%PREREQUISITES
    # NONE

%OUTPUTS
    "file lbc_$(CYCLE_TIME).um ready"
