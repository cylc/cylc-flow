# THIS IS A CYLC TASK DEFINITION FILE

#         __________________________
#         |____C_O_P_Y_R_I_G_H_T___|
#         |                        |
#         |  (c) NIWA, 2008-2010   |
#         | Contact: Hilary Oliver |
#         |  h.oliver@niwa.co.nz   |
#         |    +64-4-386 0461      |
#         |________________________|


%NAME
    coldstart, cld

%DESCRIPTION
    Supplies restart files for the main forecast model, as if it were a
    warm-started previous cycle.

%TYPE
    free, oneoff

%CYCLES
    0,6,12,18

%TASK
    coldstart.sh

%COLDSTART_PREREQUISITES
    # this task only depends on the startup task at suite start up
    # want it to have no prerequisites otherwise, so it can be inserted
    # for "live" partial suite coldstart as in 'cylc test'.
    "startup%$(CYCLE_TIME) finished"

%OUTPUTS
    "FCModel restart files ready for $(CYCLE_TIME)"
