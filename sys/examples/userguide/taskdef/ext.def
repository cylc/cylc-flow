# THIS IS A CYLC TASK DEFINITION FILE

%NAME
    get_data, X

%DESCRIPTION
    Provide the external atmospheric observations by the atmospheric model.
    Depends only on the system startup task at start time.

%TYPE
    free, contact

%CONTACT_DELAY
    1.0

%CYCLES
    0,6,12,18

%TASK
    ext.sh

%STARTUP_PREREQUISITES
    "startup%$(CYLC_TIME) finished"

%ESTIMATED_RUN_TIME
    10

%OUTPUTS
    9: "atmospheric observations ready for $(CYLC_TIME)"
