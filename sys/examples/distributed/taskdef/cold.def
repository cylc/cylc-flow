# THIS IS A CYLC TASK DEFINITION FILE

%NAME
    cold

%DESCRIPTION
    Provides restart prerequisites for the forecast task,
    which are generated by a previous forecast once the 
    system is cycling. Depends on the system startup task.

%TYPE
    free_task, oneoff

%CYCLE_TIMES
    0,6,12,18

%EXTERNAL_TASK
    cold.sh

%REMOTE_HOST
    oliverh-ws.greta

%ENVIRONMENT
    PATH $PATH:$HOME/cylc/bin:$HOME/cylc/sys/examples/distributed/scripts
    PYTHONPATH $PYTHONPATH:$HOME/cylc/src

%STARTUP_PREREQUISITES
    "startup%$(MY_CYCLE_TIME) finished" 

%PREREQUISITES
    # None

%ESTIMATED_RUN_TIME
    10

%OUTPUTS
    7: "forecast restart files ready for $(MY_CYCLE_TIME)"

%SINGLE_CYCLE
    True
