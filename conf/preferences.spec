use lockserver = boolean( default=True )
state dump directory = string( default = '.cylc/state' )
logging directory = string( default = '.cylc/logging' )
use quick task elimination = boolean( default=True )
