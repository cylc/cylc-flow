# THIS IS A CYLC TASK DEFINITION FILE


%NAME
    g_lbc_cold

%DESCRIPTION
    Get the downloaded NZ LBC file for the current cycle
    Only required at cold start startup.

%TYPE
    free, contact, oneoff

%CONTACT_DELAY
    4.0 hr

%ONEOFF_FOLLOW_ON
    g_lbc

%CYCLES
    6

%TASK
    file_transfer.sh

%ENVIRONMENT
    TARG ftpserver:/download/lbc_$(CYCLE_TIME - 6).um
    DEST hpc:/oper/input/

%PREREQUISITES
    # NONE

%ESTIMATED_RUN_TIME
    2 min

%OUTPUTS
    2 min: "file lbc_$(CYCLE_TIME - 6).um ready"
