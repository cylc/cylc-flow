# THIS IS A CYLC TASK DEFINITION FILE


%NAME
    VAR

%DESCRIPTION
    Varitional analysis

%TYPE
    free

%CYCLES
    0,6,12,18

%TASK
    VAR_nz.sh

%PREREQUISITES
    # trigger off "completed" OPS tasks, rather than particular
    # OPS outputs, so that the system is not held up by failure
    # of any one obs processing job.
    "OPS1%$(CYCLE_TIME) completed"
    "OPS2%$(CYCLE_TIME) completed"
    "OPS3%$(CYCLE_TIME) completed"

%OUTPUTS
    "VAR increments ready for $(CYCLE_TIME)"
