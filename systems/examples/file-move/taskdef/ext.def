# THIS IS A CYLC TASK DEFINITION FILE

%NAME
    ext

%DESCRIPTION
    Provides the external input required by the forecast task.
    Depends only on the system startup task at start time.

%TYPE
    free, contact

%CONTACT_DELAY
    1.0

%CYCLES
    0,6,12,18

%TASK
    ext.sh

%STARTUP_PREREQUISITES
    "startup%$(CYCLE_TIME) finished"

%ESTIMATED_RUN_TIME
    10 min

%OUTPUTS
    10 min: "external data ready for $(CYCLE_TIME)"
