# THIS IS A CYLC TASK DEFINITION FILE

%NAME
    ext

%DESCRIPTION
    Provides the external input required by the forecast task.
    Depends only on the system startup task at start time.

%TYPE
    free_task, contact

%CONTACT_DELAY
    1.0

%HOURS
    0,6,12,18

%EXTERNAL_TASK
    ext.sh

%PREREQUISITES
    # None

%STARTUP_PREREQUISITES
    "startup%$(CYLC_TIME) finished"

%ESTIMATED_RUN_TIME
    10

%OUTPUTS
    9: "external data ready for $(CYLC_TIME)"

#%SINGLE_CYCLE
#    True
