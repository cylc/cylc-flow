# THIS IS A CYLC TASK CLASS DEFINITION FILE


%TASK
    globalwave, gw

%DESCRIPTION
    The globalwave model.

%TYPE
    tied

%OWNER
    wave

%HOURS
    0

%COMMAND
    ${HOME}/bin/scripts/run_globalwave_120_only

%PREREQUISITES
    "SLS GLOBALWIND file ready for ${CYCLE_TIME}"
    "SLS GLOBALICE file ready for ${CYCLE_TIME}"

%OUTPUTS
    "file ww3g_${CYCLE_TIME}_utc_globalwave_120.nc ready for postprocessing"

%N_RESTART_OUTPUTS
    6
