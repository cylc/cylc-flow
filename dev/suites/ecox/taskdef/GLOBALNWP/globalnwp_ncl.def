# THIS IS A CYLC TASK CLASS DEFINITION FILE


%TASK
    globalnwp_ncl, g_ncl

%DESCRIPTION
    Global surface wind and pressure ncl postprocessing.

%TYPE
    free

%OWNER
    nwp

%HOURS
    0

%COMMAND
    cylc-wrapper run_vis_mslp.sh

%ENVIRONMENT
    SYS     ${USER##*_}
    SRCE_DIR    /${SYS}/ecoconnect/ecoconnect_${SYS}/output
    SRCE_FILENAME   sls_${CYCLE_TIME}_utc_global_sfcwind.nc
    
%PREREQUISITES
    "SLS GLOBALWIND file available for ${CYCLE_TIME}"

%OUTPUTS
    # default only
