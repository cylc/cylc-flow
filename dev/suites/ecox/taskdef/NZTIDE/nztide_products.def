# THIS IS A CYLC TASK CLASS DEFINITION FILE


%TASK
    nztide_products, ti_pro

%DESCRIPTION
    NZTIDE forecast product generation.
    Uses the create_images script to create the PNG areal map files, and
    uploads to Mintaka and Zope.

%TYPE
    free

%OWNER
    sea_level

%HOURS
    6,18

%ENVIRONMENT
    PRODUCT_DIR   ${HOME}/running/nztide_12/product
    SERVICE     nztide_products
     
%COMMAND
    convert_ingest.sh

%PREREQUISITES
    "nztide_ncl%${CYCLE_TIME} finished"

%OUTPUTS
    # default only
