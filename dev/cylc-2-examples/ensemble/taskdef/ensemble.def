
%FAMILY
    ensemble

%DESCRIPTION
    a family of similar tasks

%HOURS
    0,6,12,18

%MEMBERS
    C1
    C2
    C3

%PREREQUISITES
    # C1-3 depend on A, so this is redundant:
    #"A%$(CYCLE_TIME) finished"
    # But we can delay the whole family till after B for demo purposes:
    "B%$(CYCLE_TIME) finished"
