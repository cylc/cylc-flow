
0. cylc-wrapper use now explicit, not via $WRAP

1. new taskdef conditional format

2. taskdef section name changes (use util/upgrade-taskdefs.sh):
    CYCLES -> HOURS
    NAME -> TASK
    TASK -> COMMAND
    EXTRA_SCRIPTING -> SCRIPTING

3. cylcutil changes:
    cylcutil time-calc -> cylcutil cycle-time
                          cylcutil template

4. generate and modify new suite.config, rm old suite_config.py, suite_info.py, 

5. rm old task_classes.py, task_list.py, and tasks/
