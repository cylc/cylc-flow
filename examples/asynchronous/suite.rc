
description = simple asynchronous example suite

[special tasks]
    daemon = watcher

[dependencies]
    [[asynchronous:SATID]]
        graph = watcher

[tasks]
    [[watcher]]
        command = watcher.sh
