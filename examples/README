This directory contains the example suites packaged with cylc.

It is suggested that users maintain their own suites in revision control
repositories with version tagging for compatibility with particular cylc
versions.
