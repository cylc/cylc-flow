# THIS IS A CYLC TASK CLASS DEFINITION FILE

%TASK
    sls_globalice_copy, sgic

%DESCRIPTION
    Copy the converted SLS ice file to the required directory. 

%TYPE
    free

%OWNER
    wave 

%HOURS
    0

%ENVIRONMENT
    SYS     ${USER##*_}
    SRCE_FILENAME sls_${CYCLE_TIME}_utc_global_seaice.nc
    SRCE_LOC   /${SYS}/ecoconnect/ecoconnect_${SYS}/output
    DEST_FILENAME   sls_${CYCLE_TIME}_utc_global_seaice.nc
    DEST_LOC    /${SYS}/ecoconnect/wave_${SYS}/input/globalwave_120

%SCRIPTING
cp ${SRCE_LOC}/${SRCE_FILENAME} ${DEST_LOC}/${DEST_FILENAME} 

%PREREQUISITES
    "SLS GLOBALICE file available for ${CYCLE_TIME}"

%OUTPUTS
    "SLS GLOBALICE file ready for ${CYCLE_TIME}"
